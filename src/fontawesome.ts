import { library } from '@fortawesome/fontawesome-svg-core'
import {
  faRunning,
  faBars,
  faQuestionCircle,
  faList
} from '@fortawesome/free-solid-svg-icons'

library.add(
  faRunning,
  faBars,
  faQuestionCircle,
  faList
)
